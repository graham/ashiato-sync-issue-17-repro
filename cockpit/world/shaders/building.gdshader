// A BUILDING, PLAIN. See building.gdshaderinc, which is the whole of it.
shader_type spatial;
render_mode cull_back, diffuse_lambert, specular_disabled;

#include "res://world/shaders/building.gdshaderinc"
