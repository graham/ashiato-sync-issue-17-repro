// THE MOUNTAINS ON PLAIN: see mountain.gdshaderinc. No grain and no meander, which are FINE's.
shader_type spatial;
render_mode cull_back, diffuse_lambert, specular_disabled;
#include "res://world/shaders/mountain.gdshaderinc"
