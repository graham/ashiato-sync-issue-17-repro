// THE MOUNTAINS ON FINE: see mountain.gdshaderinc. The grain near the eye, and the creeks meander.
shader_type spatial;
render_mode cull_back, diffuse_lambert, specular_disabled;
#define MOUNTAIN_FINE
#include "res://world/shaders/mountain.gdshaderinc"
