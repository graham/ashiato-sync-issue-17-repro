[plugin]

name="Hand Signals"
description="Body-relative gesture zones for VR. A signal is a path through places rather than a shape to classify, so every part of it can be drawn, tested and explained."
author="godotgames"
version="0.1.0"
script="plugin.gd"
